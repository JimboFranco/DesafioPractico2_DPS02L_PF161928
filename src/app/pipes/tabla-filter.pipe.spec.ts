import { TablaFilterPipe } from './tabla-filter.pipe';

describe('TablaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TablaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
